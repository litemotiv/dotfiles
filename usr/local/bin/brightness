#!/bin/sh
dir='/sys/class/backlight/apple_backlight/brightness';
val=`cat $dir`;

case "$1" in
	up)
		sudo backlight $[val+1];
		;;
	down)
		sudo backlight $[val-1];
		;;
	*) echo "usage: $0 {up|down}"
esac
exit 0
